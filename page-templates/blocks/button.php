<section class="py-5">
    <div class="container text-center">
        <a href="<?=get_field('button')['url']?>" target="<?=get_field('button')['target']?>" class="btn btn-primary"><?=get_field('button')['title']?></a>
    </div>
</section>